ScrollText README
--

Description:
Arduino Library for displaying Scrolling Text on a single Shift Register driven Seven Segment Display.

To install:
Move ScrollText folder to arduino/libraries

To use in a project:
#include <ScrollText.h>

Instantiate between setup() and loop() with:
ScrollText whatEverNameYouWant(latchPin, clockPin, dataPin);